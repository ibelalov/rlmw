# rlmw

Reinforcement-learned minimum-weight / low-weight span-vector search.

Main artifact:

- `rlmw.ipynb`: single Google Colab notebook containing generation, discrete-gradient search, local-minimum intersection constraints, neural direction proposal, cross-entropy updates, and strategy-level Q-learning.

Persistent runtime data should be stored in Google Drive, not committed to GitHub.
